# voicecraft

## Building the app locally

```sh
npm run build:app            # builds Voicecraft.app, prints its path
npm run build:app -- --install   # also copies it into /Applications
```

No signing or notarization — for local personal use only.
